{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/publish.js": { "runtime": "nodejs20.x" }
  },
  "rewrites": [
    { "source": "/api/publish", "destination": "/api/publish" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
